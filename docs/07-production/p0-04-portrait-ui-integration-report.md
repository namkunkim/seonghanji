# P0-04 공용 초상 UI 통합 검수 보고서

> 실행일: 2026-09-01 · 상태: **기술 합격 · 수동 터치 검수 대기 없음**
> 입력: V-50 승인 세트 `ART-C901~911` · 작업 프롬프트: `p0-04-portrait-ui-integration-prompt.md`

## 1. 결과

공용 초상 11종을 실제 Godot `SC-F3` 임명 화면에 연결했다. 각 지휘부 행은 64×64 정사각 상단 크롭 칩으로 표시하며, `초상` 단추는 같은 승인 PNG를 **256×320px, 4:5** 프레임으로 연다. 프레임은 원본 PNG를 `Texture2D`로만 읽으며 재색조·합성·원본 변경을 하지 않는다.

`class[0] × disposition` 공용 매핑은 `app/views/portrait_frame.gd`에 구현했다. 제독형 실무 A/B는 `CHR-####` 숫자부 홀짝으로 분리해 재현성을 확보했고, 나머지는 V-43 표의 역할·성향 규칙대로 `C903~C911`에 연결했다.

## 2. 판정

| 항목 | 판정 | 근거 |
|---|---|---|
| 11개 승인 파일 경로 | 합격 | 자동 시험이 `ART-C901~911` 전부의 Godot 리소스 존재·Texture2D 로드를 확인 |
| 마스터 규격 | 합격 | 전부 896×1120 PNG로 로드 확인 |
| UI 프레임 | 합격 | 실제 `SC-F3`에서 64px 칩과 256×320px 프레임을 같은 매핑으로 사용 |
| 매핑 완전성 | 합격 | 제독·관료·참모·강습·파일럿 11개 분기 자동 단언 |
| 원본 보존 | 합격 | 선택 매니페스트 SHA-256과 11개 최종 PNG를 재대조해 전부 일치 |
| 기존 게임 회귀 | 합격 | Godot `run_tests.gd`: 34/34 섹션, 664 통과, 실패 0 · 헤드리스 부팅 성공 |

## 3. 실행 기록

| 항목 | 값 |
|---|---|
| 생성/GPU 비용 | **$0.00** — 기존 승인 PNG만 로드, RunPod 미사용 |
| 원본 변경 | 없음 |
| 모델 변경 | 없음 — V-50 FLUX.1 [schnell] 유지 |
| 검증 런타임 | Godot 4.7.2 headless |
| 환경 경고 | Windows 루트 인증서 저장소 읽기 경고 1건. 네트워크·리소스 로드와 무관하며 시험/부팅 결과에는 영향 없음 |

## 4. 산출물

- 프레임·결정론 매핑: `app/views/portrait_frame.gd`
- 실제 연결 지점: `app/views/fleet_screens.gd` (`SC-F3`)
- 회귀·리소스 검증: `tests/run_tests.gd` §34
- 승인 원본: `assets/preproduction/p0-04/production-v1/final/flux/`

## 5. 다음 결정

이 통합은 공용 11종의 UI 연결 검수까지만 닫는다. **명장 120명 생성은 자동으로 시작하지 않는다.** 다음 작업은 P0-08의 파일별 크레딧·라이선스 확정과 P0-09 개발 착수 게이트 잔여 결정이다. 명장 개별 초상 생산은 그 뒤 별도 승인으로만 재개한다.
