# A-05-02 — 진형 계수·상성·팔진 전투 배선

상태: PASS (2026-09-07)

`Formations`는 `FRM-01`~`FRM-07`을 한국어 저장 이름으로 정규화하고 JSON의 5페이즈 계수를 milli 정수로 제공한다. `Battle`은 기존 함종 계수에 진형·상성 계수를 정수 곱셈으로 합성한다. 일반 `Campaign._resolve_battle`은 공격 함대 및 정렬된 방어 대표 함대의 실제 `Fleet.formation`을 매 페이즈 읽는다.

③ 교전의 오각 상성은 우위측 +20%, 장사진은 −10%, 같은 진형은 0%다. 어느 쪽이든 팔진이면 상성은 무효다. 팔진은 통솔 90 이상 및 실제 `신기묘산` 특성만 허용하며 unknown ID/name은 거부한다. 저장 상태는 기존처럼 한국어 진형명만 보존한다.

검증: `test_a05_formation_combat.gd` 49/0 (ID 7종·5페이즈·상성 양방향·장사진·팔진·unknown·결정론·일반 전투 차이), 전체 `run_tests.gd` 35/35·701/0. 지형 강제와 변경 상태는 A-05-03에서 적용한다.
